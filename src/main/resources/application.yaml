spring:
  application:
    name: Foodie
  datasource:
    url: jdbc:mysql://localhost:3306/zupabase
    username: root
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    hibernate:
      ddl-auto: update
#  security:
#    user:
#      name: user
#      password: user